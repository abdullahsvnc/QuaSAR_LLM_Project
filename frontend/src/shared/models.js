export const MODELS = [
  'gemini-2.5-flash-lite', 
  'gemini-2.5-flash-lite',
]
/**
 * For now I changed the default model to the "lite" version,
 *  which is much cheaper and faster, and still performs very well.
 *  We can switch back to the full "flash" version later if needed.
 */

export const DEFAULT_MODEL_FRONT = MODELS[0]
export const FAST_MODEL_FRONT = MODELS[1]
